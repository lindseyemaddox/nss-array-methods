
console.log("hello big spenders");

// Use the filter method to get all the big spenders in the 
// main array into a new one.

// Array to contain all the big spenders
const bigSpenders = businesses.filter(business => {

})